# hermes-conduit 本地化（zh-Hans）工作量评估

> 评估日期：2026-09-11 · 基于上游 `kaishi00/hermes-conduit` @ `a0b6673`（fork：`vecStardo/hermes-conduit`）
> 测量方式：对全部 Swift 源码做字符串字面量静态扫描（脚本：`.loc_scan.py`，结果：`.loc-scan.json`）
> **2026-09-11 更新：已完成 hermes 生态参考项目复用分析（见第八节），估算已下调。**

## 一、结论（TL;DR）

| 档位 | 范围 | 单人手工 | AI 辅助（复用生态术语后） |
|---|---|---|---|
| **A 最小可用** | 基础设施 + 全部 UI 字符串本地化 + zh-Hans 翻译 + 测试修复 | 9–13 人天 | **3–5 人天** |
| **B 推荐** | A + 服务层文案/权限描述/Siri 短语/推送 + 真机验证 | 12–17 人天 | **5–7.5 人天** |
| **C 完整** | B + 语音中文链路验证 + App Store 元数据 + 文档翻译 | 14–21 人天 | 7–10 人天 |

对照方案：**硬编码直接替换中文**约 3–5 人天，但无法跟随上游更新（上游 612 commits、更新活跃）、且会破坏文案断言测试，**不建议**。

## 二、代码规模

| 项 | 数值 |
|---|---|
| 主工程 Swift | 109 个文件 / 66,204 行 |
| 测试 Swift | 106 个文件 / 71,253 行 |
| 构建方式 | xcodegen（`project.yml`，`developmentLanguage: en`） |
| 本地化基础设施 | **完全没有**（无 `.lproj` / `.strings` / `.xcstrings`） |
| CI | GitHub Actions 跑单测 + UI 测试（`ci.yml`） |

## 三、用户可见文案实测

### 字符串清点

| 类别 | 唯一条数 | 词数 | 说明 |
|---|---|---|---|
| UI API 挂钩字符串（`Text/Button/alert/navigationTitle/…`） | **683** | 3,213 | 过滤噪声后 |
| 服务层散文文案（错误提示、状态描述等） | ~163 | ~900 | `AppState`、`ConnectionFailure` 等 |
| **合计** | **≈846** | **≈4,100** | 含插值占位的 24 条（需 format 参数） |

### 分布热点（UI 字符串最多的文件）

| 文件 | UI 串数 |
|---|---|
| `Conduit/Views/AuxiliaryViews.swift`（93KB） | 167 |
| `Conduit/Views/SidebarView.swift` | 70 |
| `Conduit/Views/KanbanViews.swift` | 68 |
| `Conduit/Views/Kanban/KanbanTaskComposerView.swift` | 67 |
| `Conduit/Views/Kanban/KanbanTaskDetailView.swift` | 53 |
| `Conduit/Views/VoiceSettingsView.swift` | 42 |
| `Conduit/Services/AppState.swift`（**668KB 巨型文件**） | 31 |
| `Conduit/Views/ChatView.swift` | 31 |

### 系统级文案

- `Info.plist` **3 条权限描述**（Face ID / 麦克风 / 语音识别）→ 需 `InfoPlist.xcstrings`
- Siri **AppShortcuts**：2 条短语 + `shortTitle`（"Talk to \(.applicationName)" 等）→ 需 `AppShortcuts.xcstrings` 多语言短语
- 推送通知文案（`PushNotificationService.swift`，40KB）

## 四、语音链路中文支持评估（档 C 的核心变量）

实测结论：**语音链路对中文的耦合度低于预期，接近"可用需验证"状态**。

- ✅ 端上识别用 `Locale.current` 自适应（`AppleOnDeviceSpeechTranscriber.swift:20`），非硬编码 en-US；设备语言为中文即走中文识别（需真机验证 `supportsOnDeviceRecognition`）
- ✅ 唤醒词编译器已内置中文拼音 token 表（`WakePhraseCompiler.swift:74`）
- ✅ 服务端 TTS 已有中文音色（小米 MiMo：冰糖/茉莉/苏打/白桦）
- ⚠️ 识别/TTS 的错误提示文案为英文硬编码（已计入服务层文案）
- ⚠️ Whisper 服务端模型列表含英文限定模型（`distil-whisper-large-v3-en`），设置界面需提示用户选多语言模型

## 五、测试影响面（主要风险）

- 测试代码中**直接引用 App 文案字面量的行：1,177 行**（精确唯一交集 82 条字符串，另有 `contains` 类断言）
- 测试内 ~2,700 条散文式字面量绝大多数是 mock/fixture 数据，**不需要翻译**，只有断言 App 文案的部分需同步
- `ConduitUITests` 通过 accessibility label 定位控件 → 文案本地化后需同步更新
- CI 强制跑测试 → 本地化必须与测试修复同步推进，否则不可合并

## 六、工作分解与估算依据

| # | 工作项 | 手工 | AI 辅助 |
|---|---|---|---|
| 1 | 基础设施：`Localizable.xcstrings`（String Catalog）+ `project.yml` 配置 + `InfoPlist.xcstrings` + `AppShortcuts.xcstrings` | 1 | 0.5 |
| 2 | 846 条字符串抽取为 `String(localized:)`（大头：Views 624 条 + AppState 巨型文件） | 6–9 | 1.5–2.5 |
| 3 | 翻译 4,100 词 EN→zh-Hans + 语境审校（含 24 条插值格式串） | 2–3 | 0.5–1 |
| 4 | 测试修复（1,177 行引用面 + UI 测试） | 3–5 | 1–2 |
| 5 | 真机验证：布局截断/伪语言检查/权限提示/Siri/语音中文 | 1–2 | 1–2（真机时间不可省） |
| 6* | 附加：App Store 元数据 + README/docs（1,209 行）翻译 | +2–3 | +1–2 |

\* 档 C 追加项。

## 七、风险与建议

1. **上游活跃**（612 commits、PR 持续合入）→ 强烈建议走标准 String Catalog 方案而非硬编码替换，后续 `git merge upstream/main` 冲突面小得多。
2. **AppState.swift 单文件 668KB**，文案散落在业务逻辑中，抽取时最容易漏/错，建议 AI 辅助 + 逐段人工抽查。
3. **CI 绿线约束**：每个批次（建议按模块拆 5–6 个 PR）都需同步修复文案断言测试。
4. 复数/格式化：24 条插值串在中文语境下多数可简化（中文无复数变化），String Catalog 的 variations 按需配置即可，不必过度设计。

---
*扫描脚本 `.loc_scan.py` 与全量字符串清单 `.loc-scan.json` 保留在仓库根目录，可直接用于后续实际本地化改造。*

## 九、实施完成记录（2026-09-12）

本报告的推荐方案已全部实施完毕，实测数据如下：

| 阶段 | 结果 |
|---|---|
| 基础设施 | `Localizable.xcstrings`（974 条）+ `InfoPlist.xcstrings`（权限 3 条中英）+ `AppShortcuts.xcstrings`（Siri 短语中英），xcodegen 自动拾取 |
| 翻译 | 9 个子代理批次并行完成，986/1,112 条有中文（945 exact / 41 review / 其余为正确拒绝的不可译项），生态术语按 官方→desktop→webui 降级裁决 |
| 代码抽取 | 实测仅 554 处需要 `String(localized:)` 包裹（SwiftUI LocalizedStringKey 的 625 处自动本地化，零改动）；分类/包裹/生成全部脚本化（`docs/localization/phase2_wrap.py`、`phase3_catalog.py`） |
| 构建与验证 | **BUILD SUCCEEDED**；模拟器 zh-Hans 启动截图确认中文渲染正常 |
| 测试 | **2,237/2,237 全部通过**（修复两类问题：① 测试模拟器继承宿主中文系统语言 → `scripts/set-sim-english.sh` 固化；② iOS 26 本地化管线对 Int 插值强制 locale 分组（4242→4,242）→ 13 处调用点预格式化为 String） |

### 遗留事项

- **真机验收**（需手持设备）：权限弹窗中文、Siri 中文短语、中文语音链路（端上识别/唤醒词/TTS）、深色模式与大字号布局、87 个插值 key 的中文运行时核对（`docs/localization/phase3-interp-keys.json`）
- **润色项**：41 条 review 档译文人审（`conduit-zh-draft.json` 中 confidence=review）；「指派/分配」词系统一；跨批次拼接句衔接
- **未本地化**：推送通知正文（由 push.milim.dev 中继服务端下发，非 App 侧）；README/docs（档 C 范围，未实施）

## 八、参考项目复用分析（2026-09-11 增补）

应"用 hermes 生态项目做参考简化汉化工程"的要求，调研了三个姊妹项目的中文本地化现状，全部为 **MIT 协议，翻译语料可合法复用**：

| 参考项目 | 中文支持形态 | 规模 |
|---|---|---|
| `fathah/hermes-desktop`（桌面伴侣，14k★） | **完整 zh-CN 语言包**，按领域拆 21 个文件（kanban/sessions/tools/chat/settings…），另有 zh-TW | ~43KB 中文文案 |
| `NousResearch/hermes-agent`（官方） | 官方桌面版 `zh.ts`（209KB）+ Agent 核心 CLI `zh.yaml`（32KB） | 生态内翻译最权威 |
| `nesquena/hermes-webui`（网页端） | 单文件多语言 bundle，含 zh 在内 14 种语言 | 1.76MB |

### 实测复用效果

把三个项目的 en→zh 对照按优先级提取合并（**5,507 对，去重 4,441 条**，已存 `docs/localization/termbase-hermes.json`），与 conduit 的 1,112 条用户可见字符串（4,515 词）匹配：

- **字符串级直接命中：160 条精确 + 27 条需复核**（约占条数 17%，词数 8%）——集中在 Add/Archive/Appearance/需要审批/始终允许 这类通用短文案。conduit 的界面文案偏专用（会话分支、看板编排、语音管线设置），参考项目没有现成对应句，直接句级复用有限。
- **术语级命中才是大头：66 个核心领域术语中 47 个有现成译法**（会话/代理/工具调用/审批/看板/派发/语音/模型/提供方/档案/网关…），已整理为 [`docs/localization/术语表.md`](docs/localization/术语表.md)。
- **译法冲突按用户指定优先级自动降级，不留人工仲裁**：**官方 `hermes-agent`（桌面版 zh.ts → 核心 zh.yaml）→ `fathah/hermes-desktop` → `hermes-webui`**，高优先级来源有词条即胜出。实测 47 个术语中 31 个由官方源定稿（如 approval 官方用「审批」，压过 desktop 的「批准」）；官方未覆盖的按序降级（profile→档案、kanban→看板 取自 desktop）。
- **1,112 条字符串的翻译初稿**已按命中情况写入 [`docs/localization/conduit-zh-draft.json`](docs/localization/conduit-zh-draft.json)（exact/review/none 三档置信度），AI 翻译时以此为术语约束批量补全 none 档。

### 架构参考

三个项目均采用**按领域拆分语言文件 + key 引用 + 英文回退**的方案，与 iOS String Catalog 的组织方式同构，验证了本报告第六节的方案选择；conduit 的 String Catalog 可照搬 desktop 的领域划分（sessions/chat/tools/kanban/settings/errors…）。

### 对估算的影响

翻译初稿（原 2–3 人天手工 / 0.5–1 AI）大部分被"术语表约束 + 初稿预填"替代，术语定义工作（0.5 天）基本归零，审校因译法有据可依而提速：

- 档 A：AI 辅助 4–6 → **3–5 人天**（手工 10–15 → 9–13）
- 档 B：AI 辅助 6–9 → **5–7.5 人天**（手工 13–19 → 12–17）
- 档 C：AI 辅助 8–12 → **7–10 人天**（手工 16–24 → 14–21）

**诚实的结论**：句级复用率不高（7% 词量），省下的主要是术语定义与翻译初稿时间（约省 15–20%），真正的收益是**与 hermes 生态术语保持一致**（用户从 desktop/webui 迁移到 iOS 端零学习成本）以及大幅降低审校歧义。测试修复（1,177 行引用面）与真机验证这两块硬成本不因参考项目而减少。
